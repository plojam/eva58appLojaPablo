import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'eva58appLojaPablo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
